---
import { Image } from 'astro:assets';
export interface Props {
  src: ImageMetadata;
  alt: string;
}

const { src, alt } = Astro.props as Props;
---

<Image src={src} alt={alt} class="w-16 h-16 select-none" />
